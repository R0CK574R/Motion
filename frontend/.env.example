# Base URL of the Motion API (backend/).
# Vite only exposes vars prefixed with VITE_ to the browser — never put
# secrets here; anything in this file ships to the client.
VITE_API_URL=http://localhost:4000
