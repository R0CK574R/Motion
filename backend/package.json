{
  "name": "motion-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Motion API — auth, portfolio storage, AI analysis proxy, and Stripe billing.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate": "node -e \"require('fs').readFileSync('./db/schema.sql')\" && echo 'Run: psql $DATABASE_URL -f db/schema.sql'"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.5",
    "stripe": "^15.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
